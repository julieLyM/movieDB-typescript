{
  "devDependencies": {
    "typescript": "^3.7.5"
  },
  "scripts": {
    "start": "cd server && tsc -p . && node build/index.js ",
    "heroku-prebuild": "cd server && yarn",
    "heroku-postbuild": "cd client && yarn && yarn build"
  }
}